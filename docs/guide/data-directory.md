# 数据目录

熔岩环境管理工具将下载的软件包、绿色版服务、配置等集中在**数据目录**（根路径可在设置中修改）。

## 修改路径

1. 打开 **工具 → 数据目录**（或顶栏相关入口进入设置）。
2. 设置新的根目录路径。
3. 保存后重启应用。

::: tip
建议使用固定盘符下的短路径，例如 `D:\devenv`，避免放在需要管理员权限的目录。
:::

## 与进程的关系

- `apps` 下各子目录对应不同软件的解压结果。
- 进程配置中的可执行文件路径相对于数据目录解析。
- 迁移数据目录时，请整体复制原目录内容到新路径。
