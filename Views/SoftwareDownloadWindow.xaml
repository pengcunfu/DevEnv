<Window x:Class="DevEnv.Views.SoftwareDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DevEnv"
        mc:Ignorable="d"
        Title="软件下载工具" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 搜索和筛选区域 -->
        <GroupBox Grid.Row="0" Header="搜索与筛选" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="搜索:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Column="1" Name="TxtSearch" VerticalAlignment="Center" Margin="5"
                         TextChanged="TxtSearch_TextChanged"/>

                <TextBlock Grid.Column="2" Text="分类:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox Grid.Column="3" Name="CmbCategory" VerticalAlignment="Center" Margin="5"
                          SelectionChanged="CmbCategory_SelectionChanged"/>

                <TextBlock Grid.Column="4" Text="版本:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox Grid.Column="5" Name="CmbVersion" VerticalAlignment="Center" Margin="5"/>

                <Button Grid.Column="6" Name="BtnSearch" Content="搜索" Width="80" Height="30" Margin="5"
                        Click="BtnSearch_Click"/>
                <Button Grid.Column="7" Name="BtnReset" Content="重置" Width="80" Height="30" Margin="5"
                        Click="BtnReset_Click"/>
            </Grid>
        </GroupBox>

        <!-- 软件列表 -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,5">
                <Button Name="BtnOpenDownloadManager" Content="下载管理" Width="80" Height="28" Click="BtnOpenDownloadManager_Click"/>
            </StackPanel>
            <DataGrid Grid.Row="1" Name="SoftwareDataGrid" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  CanUserReorderColumns="False" CanUserResizeRows="False"
                  CanUserSortColumns="False" GridLinesVisibility="All"
                  HeadersVisibility="Column" IsReadOnly="False"
                  SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <!-- 软件名称 -->
                <DataGridTextColumn Header="软件名称" Binding="{Binding Name}" Width="2*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 分类 -->
                <DataGridTextColumn Header="分类" Binding="{Binding Category}" Width="1*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 描述 -->
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="2*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- 版本选择 -->
                <DataGridTemplateColumn Header="版本" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding Versions}"
                                      SelectedIndex="0"
                                      DisplayMemberPath="Version"
                                      Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 下载按钮 -->
                <DataGridTemplateColumn Header="操作" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="下载" Tag="{Binding}"
                                    Click="BtnDownload_Click" Width="80" Height="25"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        </Grid>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Name="TxtStatus" Text="就绪"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Name="TxtCount" Text="共 0 项"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
