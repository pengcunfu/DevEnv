<Window x:Class="DevEnv.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DevEnv"
        mc:Ignorable="d"
        Title="Windows 服务管理器" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="10">
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="工具(_T)">
                <MenuItem Header="JSON 格式化器(_J)" Click="OpenJsonFormatter_Click"/>
                <Separator/>
                <MenuItem Header="退出(_X)" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="帮助(_H)">
                <MenuItem Header="关于(_A)" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <!-- Title -->
        <Label DockPanel.Dock="Top"
               Content="Windows 服务管理器"
               FontSize="20" FontWeight="Bold"
               HorizontalAlignment="Center"
               Margin="0,0,0,20"/>

        <!-- Services Table -->
        <DataGrid x:Name="ServicesDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  ItemsSource="{Binding Services}">
            <DataGrid.Columns>
                <!-- Service Name -->
                <DataGridTextColumn Header="服务名称"
                                    Binding="{Binding Name}"
                                    Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Status -->
                <DataGridTextColumn Header="状态"
                                    Binding="{Binding StatusText}"
                                    Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="{Binding StatusColor, Converter={StaticResource StringToBrushConverter}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Start Button -->
                <DataGridTemplateColumn Header="启动" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="启动"
                                    Click="StartService_Click"
                                    Tag="{Binding}"
                                    Padding="10,5"
                                    IsEnabled="{Binding CanStart}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Stop Button -->
                <DataGridTemplateColumn Header="停止" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="停止"
                                    Click="StopService_Click"
                                    Tag="{Binding}"
                                    Padding="10,5"
                                    IsEnabled="{Binding CanStop}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Height="25">
            <StatusBarItem>
                <TextBlock Text="就绪"/>
            </StatusBarItem>
        </StatusBar>
    </DockPanel>
</Window>
