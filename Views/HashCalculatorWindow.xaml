<Window x:Class="DevEnv.Views.HashCalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="文件哈希计算器 - DevEnv"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        MinHeight="400" MinWidth="600"
        >

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ResultBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标签页容器 -->
        <TabControl Grid.Row="0" Margin="0,0,0,15" x:Name="MainTabControl">
            <!-- 文件哈希标签页 -->
            <TabItem Header="📁 文件哈希" x:Name="FileHashTab">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 文件选择区域 -->
                    <Border Grid.Row="0" Style="{StaticResource ResultBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     x:Name="FilePathTextBox"
                                     Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"
                                     IsReadOnly="True"
                                     VerticalAlignment="Center"/>

                            <Button Grid.Column="1"
                                    Content="📂 浏览"
                                    Style="{StaticResource ModernButtonStyle}"
                                    Command="{Binding BrowseFileCommand}"/>

                            <Button Grid.Column="2"
                                    Content="📋 粘贴文件路径"
                                    Style="{StaticResource ModernButtonStyle}"
                                    Command="{Binding PastePathCommand}"/>
                        </Grid>
                    </Border>

                    <!-- 算法选择和计算区域 -->
                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 算法选择 -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="选择哈希算法:" FontWeight="Bold" Margin="5"/>
                            <ComboBox Style="{StaticResource ModernComboBoxStyle}"
                                      x:Name="HashTypeComboBox"
                                      SelectedItem="{Binding SelectedHashType}">
                                <ComboBoxItem Content="MD5"/>
                                <ComboBoxItem Content="SHA-1"/>
                                <ComboBoxItem Content="SHA-256"/>
                                <ComboBoxItem Content="SHA-384"/>
                                <ComboBoxItem Content="SHA-512"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- 计算按钮 -->
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="计算操作:" FontWeight="Bold" Margin="5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0"
                                        Content="🔢 计算哈希"
                                        Style="{StaticResource ModernButtonStyle}"
                                        Command="{Binding CalculateHashCommand}"/>

                                <Button Grid.Column="1"
                                        Content="🔢 全部算法"
                                        Style="{StaticResource ModernButtonStyle}"
                                        Command="{Binding CalculateAllHashesCommand}"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- 拖拽区域 -->
                    <Border Grid.Row="2"
                            Style="{StaticResource ResultBorderStyle}"
                            Margin="0,10,0,0"
                            AllowDrop="True"
                            Drop="Border_Drop"
                            DragEnter="Border_DragEnter"
                            DragLeave="Border_DragLeave">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="📁" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                            <TextBlock Text="拖拽文件到此处"
                                       FontSize="16"
                                       HorizontalAlignment="Center"
                                       Opacity="0.6"
                                       Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- 文本哈希标签页 -->
            <TabItem Header="📝 文本哈希" x:Name="TextHashTab">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 输入文本区域 -->
                    <TextBlock Grid.Row="0" Text="输入文本内容:" FontWeight="Bold" Margin="5"/>
                    <TextBox Grid.Row="1"
                             Style="{StaticResource ModernTextBoxStyle}"
                             x:Name="TextInputTextBox"
                             Text="{Binding TextInput, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             AcceptsReturn="True"
                             Height="150"/>

                    <!-- 计算按钮 -->
                    <Button Grid.Row="2"
                            Content="🔢 计算文本哈希"
                            Style="{StaticResource ModernButtonStyle}"
                            Command="{Binding CalculateTextHashCommand}"
                            Margin="5,10,5,5"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- 进度条 -->
        <Grid Grid.Row="1" x:Name="ProgressGrid" Visibility="Collapsed" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ProgressBar Grid.Row="0"
                         Height="20"
                         Minimum="0"
                         Maximum="100"
                         Value="{Binding Progress}"/>

            <TextBlock Grid.Row="1"
                       Text="{Binding ProgressText}"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </Grid>

        <!-- 结果显示区域 -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="0,10,0,0">
            <ItemsControl ItemsSource="{Binding HashResults}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ResultBorderStyle}" Margin="0,5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 算法名称 -->
                                <TextBlock Grid.Row="0"
                                           Text="{Binding Algorithm}"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           Margin="0,0,0,5"/>

                                <!-- 哈希值和复制按钮 -->
                                <Grid Grid.Row="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0"
                                             Text="{Binding Hash}"
                                             IsReadOnly="True"
                                             Background="White"
                                             FontFamily="Consolas"
                                             FontSize="12"
                                             VerticalAlignment="Center"
                                             Margin="0,0,5,0"/>

                                    <Button Grid.Column="1"
                                            Content="📋 复制"
                                            Style="{StaticResource ModernButtonStyle}"
                                            Command="{Binding DataContext.CopyHashCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding Hash}"/>
                                </Grid>

                                <!-- 文件信息 -->
                                <StackPanel Grid.Row="2"
                                            Orientation="Horizontal"
                                            Margin="0,5,0,0"
                                            Opacity="0.7">
                                    <TextBlock Text="文件大小: "/>
                                    <TextBlock Text="{Binding FileSize, Converter={StaticResource FileSizeConverter}}"/>
                                    <TextBlock Text="  |  " Margin="10,0,10,0"/>
                                    <TextBlock Text="计算用时: "/>
                                    <TextBlock Text="{Binding CalculationTime, StringFormat='{}{0:fff} ms'}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="3" Margin="0,15,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>