<Window x:Class="DevEnv.Views.HostsFileEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hosts 文件编辑器" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="文本编辑">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0"
                             Text="{Binding HostsText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="重新加载" Width="100" Height="30"
                                Command="{Binding ReloadCommand}" Margin="0,0,10,0"/>
                        <Button Content="保存" Width="100" Height="30"
                                Command="{Binding SaveTextCommand}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="按条编辑">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="0"
                              ItemsSource="{Binding Entries}"
                              SelectedItem="{Binding SelectedEntry}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="False"
                              CanUserResizeRows="False"
                              CanUserSortColumns="False"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              SelectionMode="Single"
                              Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="IP地址" Binding="{Binding Ip}" Width="2*"/>
                            <DataGridTextColumn Header="域名" Binding="{Binding Domain}" Width="3*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="添加" Width="80" Height="30"
                                Command="{Binding AddEntryCommand}" Margin="0,0,10,0"/>
                        <Button Content="删除" Width="80" Height="30"
                                Command="{Binding DeleteEntryCommand}" Margin="0,0,10,0"/>
                        <Button Content="修改" Width="80" Height="30"
                                Command="{Binding ModifyEntryCommand}"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,6">
            <Button Content="定位到文件" Width="100" Height="30"
                    Command="{Binding LocateFileCommand}" Margin="0,0,8,0"/>
            <Button Content="在记事本中打开" Width="120" Height="30"
                    Command="{Binding OpenInNotepadCommand}"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,6,0,0">
            <TextBlock Text="就绪" FontSize="11" Foreground="#9CA3AF"/>
        </Border>
    </Grid>
</Window>
